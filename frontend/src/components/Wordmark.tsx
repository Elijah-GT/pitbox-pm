/**
 * The wordmark, as a standalone piece so the public pages and the app cannot
 * drift apart. Type only — the mountain glyph that used to lead it is gone.
 */
export function Wordmark() {
  return (
    <span className="wordmark-text">
      PIT <b>BOX</b>
    </span>
  )
}
